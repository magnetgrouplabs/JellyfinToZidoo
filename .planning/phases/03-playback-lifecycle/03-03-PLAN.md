---
phase: 03-playback-lifecycle
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [PLAY-01, PLAY-02, PLAY-03, PLAY-04, PLAY-05, PLAY-06]

must_haves:
  truths:
    - "Jellyfin dashboard shows Now Playing during active Zidoo playback"
    - "Stopping playback mid-movie saves resume position visible in Jellyfin client"
    - "Watching 90%+ marks as watched in Jellyfin client"
    - "After playback, Jellyfin client relaunches automatically"
  artifacts: []
  key_links: []
---

<objective>
Deploy the app to a real Zidoo device and verify the full playback lifecycle works end-to-end.

Purpose: Confirms all playback reporting actually works with real Jellyfin server and Zidoo player.
Output: Verified working playback lifecycle on device.
</objective>

<execution_context>
@C:/Users/anthony/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anthony/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-playback-lifecycle/03-CONTEXT.md
@.planning/phases/03-playback-lifecycle/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy APK to Zidoo device</name>
  <files></files>
  <action>
    1. Build debug APK: `./gradlew assembleDebug -x lint`
    2. Connect to Zidoo device: `adb connect 192.168.0.71:5555`
    3. Install APK: `adb -s 192.168.0.71:5555 install -r app/build/outputs/apk/debug/app-debug.apk`
    4. Verify installation: `adb -s 192.168.0.71:5555 shell pm list packages | grep jellyfintozidoo`
  </action>
  <verify>
    <automated>cd /c/Users/anthony/JellyfinToZidoo && adb -s 192.168.0.71:5555 shell pm list packages | grep jellyfintozidoo</automated>
  </verify>
  <done>APK installed on Zidoo device at 192.168.0.71.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full playback lifecycle on device</name>
  <action>
    User verifies the complete playback lifecycle on real hardware. Claude has already built and deployed the APK in Task 1.
  </action>
  <verify>User confirms all 4 E2E tests pass.</verify>
  <done>All playback lifecycle behaviors verified on real Zidoo device with real Jellyfin server.</done>
  <what-built>Complete playback lifecycle: start reporting, progress polling, stop reporting, watched threshold, and client relaunch.</what-built>
  <how-to-verify>
    Test 1 -- Now Playing (PLAY-01, PLAY-02):
    1. Open your Jellyfin client on the Zidoo and play a movie
    2. While the Zidoo player is active, check the Jellyfin dashboard (web UI on another device)
    3. Verify it shows "Now Playing" with the correct title and updating position

    Test 2 -- Resume Position (PLAY-03, PLAY-04):
    1. Stop playback mid-movie (press Back or Stop on remote)
    2. Check the movie in your Jellyfin client
    3. Verify the resume position matches approximately where you stopped

    Test 3 -- Watched Status (PLAY-05):
    1. Play a short video or skip to near the end (past 90%)
    2. Let it finish or stop past 90%
    3. Check Jellyfin -- the item should be marked as watched (checkmark)

    Test 4 -- Client Relaunch (PLAY-06):
    1. Play a movie from a Jellyfin client (e.g., Findroid, jellyfin-androidtv)
    2. After playback ends, check if the Jellyfin client relaunches
    3. If it does not relaunch (returns to Android TV home instead), note which client you used -- this is expected with some clients that use startActivity() instead of startActivityForResult()
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 tests pass, or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
- APK installed and running on Zidoo device
- All 4 E2E tests verified by user on real hardware
</verification>

<success_criteria>
- Jellyfin dashboard shows Now Playing during Zidoo playback
- Resume position saved correctly on stop
- 90%+ playback marks item as watched
- Client relaunches after playback (or graceful fallback documented)
</success_criteria>

<output>
After completion, create `.planning/phases/03-playback-lifecycle/03-03-SUMMARY.md`
</output>
